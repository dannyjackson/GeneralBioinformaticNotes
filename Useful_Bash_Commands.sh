# Useful Bash Commands
less file.txt       # lets you read an entire file in an interactive way without outputting it all to the 
du -hs /path/to/directory/*        # shows sizes of all files in the current directory; use du -hs /path/to/directory/ to get total size of a directory
